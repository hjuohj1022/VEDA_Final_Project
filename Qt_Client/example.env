# ================================
# 백엔드 API 서버 설정
# ================================
# 로그인/녹화목록/파일관리 API 주소
API_URL=https://your-nginx-gateway-ip:443

# ================================
# RTSP (MediaMTX) 설정
# ================================
# MediaMTX 서버 IP 또는 도메인 (Nginx Gateway IP)
RTSP_IP=your-nginx-gateway-ip
# MediaMTX 서버 포트 (RTSP: 8554 / RTSPS: 8555)
RTSP_PORT=8554

# 채널별 메인/서브 경로 템플릿 ({index}는 0~3으로 치환)
RTSP_MAIN_PATH_TEMPLATE=/{index}/main
RTSP_SUB_PATH_TEMPLATE=/{index}/sub

# 프로파일 번호 (클라이언트 내부 구분용)
RTSP_MAIN_PROFILE=1
RTSP_SUB_PROFILE=2

# ================================
# MQTT 설정
# ================================
# MQTT 브로커 주소/포트 (Nginx Gateway IP)
MQTT_HOST=your-nginx-gateway-ip
MQTT_PORT=8883

# TLS 사용 여부 (1: 사용, 0: 미사용)
MQTT_USE_TLS=1

# MQTT 기능 사용 여부 (1: 사용, 0: 미사용)
MQTT_ENABLED=1

# TLS 인증서 경로 (실행 파일 기준 상대 경로 또는 절대 경로)
MQTT_CA_CERT=certs/rootCA.crt
MQTT_CLIENT_CERT=certs/client-qt.crt
MQTT_CLIENT_KEY=certs/client-qt.key

# 관리자 기능 잠금 해제 키
ADMIN_UNLOCK_KEY=your-admin-unlock-key
