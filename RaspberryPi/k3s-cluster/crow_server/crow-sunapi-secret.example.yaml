apiVersion: v1
kind: Secret
metadata:
  name: crow-sunapi-secret
  namespace: default
type: Opaque
stringData:
  # 카메라 계정 정보는 Git에 커밋 금지
  SUNAPI_USER: "REPLACE_CAMERA_USER"
  SUNAPI_PASSWORD: "REPLACE_CAMERA_PASSWORD"

